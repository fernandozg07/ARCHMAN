{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --settings=config.settings_simple && python manage.py runserver 0.0.0.0:$PORT --settings=config.settings_simple",
    "healthcheckPath": "/api/health/"
  }
}